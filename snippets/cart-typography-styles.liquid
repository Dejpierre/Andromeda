{%- doc -%}
  Placeholder restoration — the original cart-typography-styles.liquid content was not
  recoverable (it existed only on the live theme, never synced to this repo, and was
  accidentally deleted). This empty stub only stops the Liquid error; it applies no
  typography overrides. Re-add the real styles if cart typography looked different
  from the theme default.
{%- enddoc -%}
