{%- doc -%}
  Placeholder restoration — the original text-component-shimmer-styles.liquid content
  was not recoverable (it existed only on the live theme, never synced to this repo,
  and was accidentally deleted). This empty stub only stops the Liquid error; the
  `<text-component shimmer>` loading effect renders without its dedicated animation
  styles. Re-add the real styles if the shimmer/loading effect looked different.
{%- enddoc -%}
