{%- doc -%}
  Placeholder restoration — the original summary-icon-styles.liquid content was not
  recoverable (it existed only on the live theme, never synced to this repo, and was
  accidentally deleted). This empty stub only stops the Liquid error; it applies no
  custom styling to the mobile menu drawer's <summary> disclosure icons (falls back
  to the theme default). Re-add the real styles if those icons looked different.
{%- enddoc -%}
