{%- doc -%}
  Placeholder restoration — the original chat-drawer.liquid content was not
  recoverable (it existed only on the live theme, never synced to this repo,
  and was accidentally deleted). This empty stub only stops the Liquid error;
  it renders no chat widget. Re-add the real markup if this snippet was
  powering an actual chat feature.
{%- enddoc -%}
