{%- doc -%}
  Placeholder restoration — the original cart-disclosure-tooltip.liquid content was
  not recoverable (it existed only on the live theme, never synced to this repo, and
  was accidentally deleted). This empty stub only stops the Liquid error; it renders
  no tooltip/disclosure next to the cart item title. Re-add the real markup if this
  snippet was showing per-item extra info (e.g. size, care instructions).

  @param {object} [product] - The line item's product.
  @param {string} [unique_id] - Shared id linking the trigger and its dialog.
  @param {string} [mode] - 'trigger' or 'dialog'.
{%- enddoc -%}
